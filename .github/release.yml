name: Package and Release Skill

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  package-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Package skill
        run: |
          zip -r ios-architecture-expert.skill \
            SKILL.md \
            references/ \
            .claude-plugin/ \
            README.md \
            LICENSE \
            -x "*.git*"
          ls -lh ios-architecture-expert.skill

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ios-architecture-expert.skill
          generate_release_notes: true
          body: |
            ## iOS Architecture Expert Skill

            Guides building modular, testable iOS/Swift applications following the Essential Developer methodology — clean architecture with protocol-oriented design, composition root, generic presenters, and modern Swift concurrency.

            ### Installation

            **Claude Code / skills CLI (recommended):**
            ```bash
            npx skills add SwiftyJourney/ios-architecture-expert-skill
            ```

            **Manual:**
            ```bash
            git clone https://github.com/${{ github.repository }}.git
            ```

            See [README.md](https://github.com/${{ github.repository }}/blob/main/README.md) for full details.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact (manual dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: ios-architecture-expert-skill
          path: ios-architecture-expert.skill
